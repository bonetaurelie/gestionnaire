{% extends 'ABPlatformBundle::layout.html.twig' %}

{% block title %}
    Création d'un espace client - {{ parent() }}
{% endblock %}

{% block body %}
<h1>Enregistrement du nouveau client:</h1>
    {{ form_start(form, {'attr':{'class':'form-horizontal'}}) }}
    {{ form_errors(form) }}

    <div class="form-group">
        {{ form_label(form.pseudo, 'Votre pseudo', {'attr':{'class':'col-md-6 control-label'}}) }}
        {{ form_errors(form.pseudo) }}
        <div class="col-md-6">
            {{ form_widget(form.pseudo, {'attr': {'class':'form-control'}}) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(form.password, 'Votre mot de passe', {'attr':{'class':'col-md-6 control-label'}}) }}
        {{ form_errors(form.password) }}
        <div class="col-md-6">
            {{ form_widget(form.pasword, {'attr': {'class':'form-control'}}) }}
        </div>
    </div>

    <div class="col-md-offset-6">
        {{ form_widget(form.valider, {'attr': {'class':'btn-primary btn-lg'}}) }}
        <a class="btn-danger btn-lg col-md-offset-1" href="{{ path('ab_platform_read') }}">Annuler</a>
    </div>

    {{ form_rest(form) }}
    {{ form_end(form) }}
{% endblock %}